# タスク

## 1. 共有部品の抽出(既存挙動を変えない準備)

- [x] 1.1 エピソードの状態からアクション種別(DL中/現在/再生可/失敗/未DL)を決める判断を `EpisodeRow.kt` の LeadingAction から純粋関数へ切り出し、`logic/` に置く(Android API 依存ゼロで書けることを確認する。無理なら ui/ 内の共有関数に留め、理由を記録)
- [x] 1.2 切り出した判断の JVM ユニットテストを全状態の組み合わせで書く(状態空間は列挙可能なので全数列挙)
- [x] 1.3 `EpisodeRow.kt` を切り出した判断を使う形に置き換え、一覧の見た目・挙動が変わらないことを確認する
- [x] 1.4 視聴済みの pendingUndo(`UndoablePlayedChange`)を `EpisodeListViewModel` ローカルから app スコープの共有ホルダーへ引き上げる(スナックバー表示は当面一覧のまま。挙動不変)

## 2. 統合エピソード画面の実装

- [x] 2.1 `EpisodeDetailViewModel` を拡張する: playback・downloader・networkState・feedRepository・artworkStore を受け取り、S=C 判定(表示中IDと再生状態IDの一致)・アートワーク読み込みを追加する
- [x] 2.2 再生開始を実装する: DB 保存のフィルターから表示順を再現し `PlaybackQueue.playbackOrderFrom` でキューを構築、空ならフォールバック文言(フィルター除外・未DLのケース)
- [x] 2.3 DL 操作を実装する: 回線判定と確認ダイアログ(`DownloadConfirmation` と同型)・進捗・失敗時再試行を一覧と同じ規則で
- [x] 2.4 `EpisodeDetailScreen` に状態別アクション領域を実装する: 現在ならフルコントロール(シークバー・±10秒・速度)、それ以外は 1.1 の判断による大きなアクション。タイトル・ショーノート・アートワーク・視聴済みトグルは全状態で表示
- [x] 2.5 `PlayerScreen` から流用する部品(ProgressSection・TransportControls・SpeedSelector・formatTime)を統合画面へ移す

## 3. ナビゲーションの切り替え

- [x] 3.1 ミニプレイヤーのタップ先を `episodes/{現在のエピソードID}` への push 遷移に変更する
- [x] 3.2 追随を実装する: S=C の画面表示中に再生状態のエピソードIDが別の非null値へ遷移したら、nav エントリを次のIDの統合画面へ置き換える(スタックに積まない)。キュー終端(null への遷移)では何もしない
- [x] 3.3 ミニプレイヤーの表示規則を「再生中かつ、現在エントリが現在のエピソードの統合画面でないとき」に変更する(backstack エントリの episodeId 引数と再生状態IDの比較)
- [x] 3.4 `player` route と `PlayerScreen.kt` を削除する(自動クローズの LaunchedEffect・二重popガードも消えることを確認)
- [x] 3.5 実機で確認する: 追随時の置き換えアニメーションが目障りでないこと、回転・プロセス復元で表示中エピソードが維持されること、キュー終端で画面が留まりノートのみの姿になること
  - キュー終端で画面が留まりノートのみの姿になること: 確認済み(視聴済み化+DL自動削除まで想定どおり)
  - 追随の置き換え: 確認済み(#45 の終端で画面が #44 に置き換わり再生継続。戻る操作で一覧に着地し、聴き終えた回が履歴に積み増されない)
  - 回転・プロセス復元: 確認済み(横向き・プロセス kill 後の再起動のいずれでも同じエピソードの画面が維持される)

## 4. 「再生中の視聴済み化=停止」の統一

- [x] 4.1 `PlaybackConnection` に「再生を停止しキューを空にする」操作を追加する(player 層は played を観測しない構造を維持し、停止は UI からの明示操作とする)
- [x] 4.2 統合画面の視聴済みトグル: 非再生中は従来の記録+Undo、現在のエピソードなら視聴済み化+停止+前の画面へ pop+スナックバー(文言に「再生を停止しました」を含める)
- [x] 4.3 一覧の再生中の行のトグルにも同じ停止作用を足す(画面遷移なし)
- [x] 4.4 Undo が記録のみ復元する(再生を自動再開しない)ことと、pop 後の遷移先でスナックバーが表示・操作できることを確認する
- [x] 4.5 実機で確認する: 停止→猶予経過→削除の順で鳴っている最中のファイル削除が起きないこと、Undo 後に▶で保存位置から再開できること

## 5. 品質ゲートと仕上げ

- [x] 5.1 `./gradlew check` を通す(ktfmt・detekt・Lint・カバレッジ。logic/ 追加分は 90% 以上)
- [x] 5.2 計装テストが必要な範囲を確認し、必要なら `connectedAndroidTest` を流す(`.instrumented` 別パッケージで実行されること)
- [x] 5.3 `installDebug` で実機に上書きインストールし、購読・視聴状態・DLファイルが保持されたまま新画面が動くことを通しで確認する(読む→DL→再生→視聴済み停止→Undo)
